#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# fail fast
set -e

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path
# parse args
BUILD_DIR=$1
CACHE_DIR=$2

if [ ! -f $BUILD_DIR/application.properties ] ; then
    echo "-----> application.properties not found. This file is required. Build failed."
    echo " !     Failed to build app"
    exit 1
fi
    
GRAILS_VERSION=`grep app\.grails\.version $BUILD_DIR/application.properties | sed -E -e 's/app\.grails\.version[ \t]*=[ \t]*([^ \t]+)[ \t]*$/\1/g'`

echo "-----> Grails $GRAILS_VERSION app detected"

if [ ! "$GRAILS_VERSION" == "1.3.7" ] && [ ! "$GRAILS_VERSION" == "2.0.0.RC1" ] ; then
    echo "-----> Grails version $GRAILS_VERSION not supported."
    echo "       Only Grails 1.3.7 and 2.0.0.RC1 are currently supported"
    echo " !     Failed to build app"
    exit 1
fi

if [ ! -d $CACHE_DIR ] ; then
  mkdir $CACHE_DIR
fi

cd $CACHE_DIR

# install grails
GRAILS_URL="http://lang-pack-grails.s3.amazonaws.com/grails-$GRAILS_VERSION.tar.gz"

if [ ! -d "grails-$GRAILS_VERSION" ]; then
  echo -n "-----> Installing Grails $GRAILS_VERSION....."
#  curl --silent --max-time 60 --location $GRAILS_URL | tar xz
  curl --silent --location $GRAILS_URL | tar xz
  echo " done"
fi

# This is a simplisitic way to only set JAVA_HOME when on Cedar. If you run this 
# script locally and your jdk is not in this location, you're responsible for
# setting it yourself. Need to find a smarter way of conditionally setting this var
if [ -d /usr/lib/jvm/java-6-openjdk ] ; then
    export JAVA_HOME=/usr/lib/jvm/java-6-openjdk
fi
export GRAILS_HOME=$CACHE_DIR/grails-$GRAILS_VERSION
export PATH=$GRAILS_HOME/bin:$PATH

cd $BUILD_DIR

# build app
if [ "$GRAILS_VERSION" = "2.0.0.RC1" ] ; then
    BUILDCMD_OPT="-plain-output"
fi
BUILDCMD="grails $BUILDCMD_OPT -Divy.default.ivy.user.dir=$CACHE_DIR war"

echo "-----> executing $BUILDCMD"

$BUILDCMD 2>&1 | sed -u 's/^/       /'

if [ "${PIPESTATUS[*]}" != "0 0" ]; then
  echo " !     Failed to build app"
  exit 1
fi

if [ ! -d server ] ; then
    echo "-----> No server directory found. Adding jetty-runner 7.5.0.v20110901 automatically."
    mkdir server
    cp $GRAILS_HOME/jetty-runner-7.5.0.v20110901.jar server/jetty-runner.jar
fi


